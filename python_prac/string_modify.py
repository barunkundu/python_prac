""" .upper = returns string in upper case
    .lower = returns string in lower case
    .replace = replace any alphabet 
    [which we replace it weite in 1st and which we input write in last]
    .strip = replace whitspace beginning and ending
    .split = returns a list where the text between the specified separator becomes the list items.
     although it give us a list 
    .startwith("latter") = returns which latters are 
     strat in this latter
     """

a = "Bananna Apple"
# print(a.upper())

# print(a.lower())
# print(a.replace('B','b'))

print(a.split())

b = " kundu "
print(b.strip())


# combine two string 

c=a+b
print(c)

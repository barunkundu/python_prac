''' there are 2 types of modules 
    1. build in (which need not to be install)
    2. external (which need to be install)'''

# generally we use "pip install ........" to install 
# any external file(work in terminal)

